name: example
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl:

dev_dependencies:
  custom_lint:
  sonnet_linter:
    path: ../
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true
